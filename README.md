# Node.js & Docker Deployment Assignment

A simple Node.js (Express) app, containerized with Docker and pushed to Docker Hub.

## App Overview
- `GET /` – returns a hello message
- `GET /health` – returns a JSON health check

## Run Locally (without Docker)